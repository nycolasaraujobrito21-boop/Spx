local ReplicatedStorage = game:GetService("ReplicatedStorage") local RemoteGerenciarBola = ReplicatedStorage:WaitForChild("RemoteGerenciarBola")  -- Permite controle da bola sem abusos RemoteGerenciarBola.OnServerEvent:Connect(function(player, acao, dados)     local character = player.Character     if not character then return end     local rootPart = character:FindFirstChild("HumanoidRootPart")     if not rootPart then return end      local bola = game.Workspace:FindFirstChild(dados.nomeBola)     if not bola or not bola:IsA("BasePart") then return end      -- Verifica distância máxima permitida     local distancia = (rootPart.Position - bola.Position).Magnitude     if distancia > 10 then return end      -- Aplica ação na bola     if acao == "PEGAR" then         bola.Anchored = false         bola.Velocity = Vector3.new(0, 0, 0)     elseif acao == "CONDUZIR" then         bola.Positnd)
